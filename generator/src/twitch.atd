type date = string wrap <ocaml module="Lenient_iso8601">

type video = {
    video_id: string;
    title: string;
    url: string;
    date: date;
    duration: float;
    thumbnail: string;
}

type videos = video list
