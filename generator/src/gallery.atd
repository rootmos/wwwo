type date = string wrap <ocaml module="Lenient_iso8601">

type entry = {
    id: string;
    url: string;
    content_type: string;
    last_modified: date;
    ?thumbnail: string option;
}

type entries = entry list
