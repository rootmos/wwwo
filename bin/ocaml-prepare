#!/bin/bash

set -o nounset -o pipefail -o errexit

PROJECT=${1-.}

opam init --bare --disable-sandboxing --shell-setup

SWITCH=$(cat "$PROJECT/switch")
echo 1>&2 "preparing switch: $SWITCH"
opam switch create "$SWITCH"
